<body id="homepageBody">
    <section id="titleHeader" class="">
        <h1 id="titleHeaderText" class="">Rankedify</h1>
        <div id="nav" class="">
            <nav>
                {{#if loggedIn}}
                <button>Log out</button>
                {{else}}
                <button '/login'>Log in</button>
                {{/if}}
                <button>My list</button>
                <button>About us</button>
            </nav>
        </div>
        <div id="intro">Choose your favorite games and share them with the world</div>
        <div id="intro2"><span id="homeLogIn"><a id='spanLogIn' href="#">Log in </a></span>and start sharing
        </div>
    </section>
</body>