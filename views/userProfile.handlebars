<div id="profileBody">
    <nav class="userProfileNav justify-end  p-2" id="userProfileNavBar">
        <a type="button" id="userProfileNavBTN" class=" btn hover:text-black btn-outline-dark" href="/"
            id="HomeBTN">Home</a>
        {{#if loggedIn}}
        <a type="button" id="userProfileNavBTN" class="  btn hover:text-black btn-outline-dark" href="/">Log
            Out</a>
        {{/if}}
        <a type="button" id="userProfileNavBTN" class="  btn hover:text-black btn-outline-dark" href="/list">User Picks</a>
        <a type="button" id="userProfileNavBTN" class="  btn hover:text-black btn-outline-dark" href="/aboutus">About
            us</a>
        <a type="button" id="userProfileNavBTN" class="  btn hover:text-black btn-outline-dark" href="/toppicks">Top
            Picks</a>
    </nav>
    <h1 id="titleHeaderTextLogin" class="d-flex">
        <span class="userProfileTitle text-bold" id="span">
            Rankedify
        </span>
    </h1>
        
            <div>
                <div id="">

                    <ul id="allListUlBody">
                    <div class="UIUsername">
                        {{#if list.user_input}}
                        <b>{{list.username}}'s List</b>
                        {{/if}}
                    </div>    
                        <li>1. {{list.user_input.game_input_one}}</li>
                        <li>2. {{list.user_input.game_input_two}}</li>
                        <li>3. {{list.user_input.game_input_three}}</li>
                        <li>4. {{list.user_input.game_input_four}}</li>
                        <li>5. {{list.user_input.game_input_five}}</li>

                        {{#if list.user_input}}
                        <button id="deleteList" class="uPBtn hover:text-black btn-outline-dark">Delete Your
                            List</button>
                        {{else}}
                        <a type="button" id="createList" class="btn btn-outline-dark" href="/userinput">Click here to
                            create a
                            list</a>
                        {{/if}}
                    </ul>
                </div>
            </div>
        
</div>

{{#if loggedIn}}
<script src="/js/mylist.js"></script>
<script src="/js/logout.js"></script>
{{/if}}
<script src="/js/login.js"></script>
<script src="/js/delete.js"></script>
</div>